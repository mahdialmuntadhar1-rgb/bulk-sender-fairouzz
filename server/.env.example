# Nabda WABA secure API Key - Obtain from https://nabdaotp.com dashboard.
# Do NOT prefix with Bearer! Put the raw hex key here.
NABDA_API_KEY=your_raw_api_key_here

# Port for local proxy server (defaults to 3001 to avoid clashing with frontend)
PORT=3001
